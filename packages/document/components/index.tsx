// 生成一个react组件
import { TimelineEditor } from '@xzdarcy/react-timeline-editor';
import React from 'react';

// 为了文档生成timeline默认的类型，所以这里引入timeline类型并导出默认组件
export const Timeline: React.FC<TimelineEditor> = () => {
  return <div></div>;
};
